<Window x:Class="GleemLet.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        xmlns:views="clr-namespace:GleemLet.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="GleemLet" Height="720" Width="1100"
        MinHeight="600" MinWidth="800"
        Background="{DynamicResource Bg}"
        WindowStyle="SingleBorderWindow"
        TextOptions.TextFormattingMode="Display"
        KeyDown="Window_KeyDown">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
    </Window.Resources>

    <Border Background="{DynamicResource Bg}" BorderBrush="{DynamicResource Border}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="220"/>  <!-- Sidebar -->
                <ColumnDefinition Width="*"/>    <!-- Content -->
            </Grid.ColumnDefinitions>

            <!-- ═══════════ SIDEBAR ═══════════ -->
            <Border Grid.Column="0" Background="{DynamicResource Sidebar}" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="56"/>  <!-- Logo + titlebar -->
                        <RowDefinition Height="*"/>   <!-- Nav -->
                        <RowDefinition Height="Auto"/><!-- Profile -->
                    </Grid.RowDefinitions>

                    <!-- Logo (draggable titlebar) -->
                    <Border Grid.Row="0" MouseLeftButtonDown="TitleBar_Drag"
                            shell:WindowChrome.IsHitTestVisibleInChrome="True">
                        <Grid Margin="16,0">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <!-- Styled circular logo -->
                                <Border Width="32" Height="32" CornerRadius="16" Margin="0,0,10,0">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#2D7A58" Offset="0"/>
                                            <GradientStop Color="#5CDEB5" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.Effect>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="0" Color="#4FAC82" Opacity="0.7"/>
                                    </Border.Effect>
                                    <TextBlock Text="G" FontFamily="Segoe UI" FontSize="15" FontWeight="Bold"
                                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <TextBlock FontFamily="Segoe UI" FontSize="16" FontWeight="Bold" Foreground="{DynamicResource Text}" VerticalAlignment="Center">
                                    <Run Text="Gleem"/><Run Text="Let" Foreground="#4FAC82"/>
                                </TextBlock>
                            </StackPanel>
                            <!-- Window controls -->
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center"
                                        shell:WindowChrome.IsHitTestVisibleInChrome="True">
                                <Button Content="—" Style="{StaticResource IconButton}" Width="24" Height="24" FontSize="12" Click="MinBtn_Click"/>
                                <Button Content="✕" Style="{StaticResource IconButton}" Width="24" Height="24" FontSize="11" Click="CloseBtn_Click" Margin="2,0,0,0"/>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Nav items -->
                    <StackPanel Grid.Row="1" Margin="10,10,10,0">
                        <TextBlock Text="MENU" FontFamily="Segoe UI" FontSize="10" FontWeight="SemiBold"
                                   Foreground="{DynamicResource Dim}" Margin="6,0,0,8"/>

                        <ToggleButton x:Name="NavHome" IsChecked="True"
                                      Style="{StaticResource NavButton}" Click="Nav_Click" Tag="home">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Home" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="NavHomeText" Text="Home" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>

                        <ToggleButton x:Name="NavSets" Style="{StaticResource NavButton}" Click="Nav_Click" Tag="sets" Margin="0,2,0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="BookOpenPageVariant" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="NavSetsText" Text="My Sets" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>

                        <ToggleButton x:Name="NavStats" Style="{StaticResource NavButton}" Click="Nav_Click" Tag="stats" Margin="0,2,0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ChartBar" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="NavStatsText" Text="Statistics" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>

                        <ToggleButton x:Name="NavBadges" Style="{StaticResource NavButton}" Click="Nav_Click" Tag="badges" Margin="0,2,0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Medal" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="NavBadgesText" Text="Badges" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>

                        <ToggleButton x:Name="NavProfile" Style="{StaticResource NavButton}" Click="Nav_Click" Tag="profile" Margin="0,2,0,0">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Account" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="NavProfileText" Text="Profile" VerticalAlignment="Center"/>
                            </StackPanel>
                        </ToggleButton>

                        <Separator Background="{DynamicResource Border}" Margin="6,16,6,16"/>

                        <TextBlock Text="STUDY" FontFamily="Segoe UI" FontSize="10" FontWeight="SemiBold"
                                   Foreground="{DynamicResource Dim}" Margin="6,0,0,8"/>

                        <Button Style="{StaticResource GhostButton}" Click="QuickStudy_Click"
                                Margin="0,0,0,4" HorizontalContentAlignment="Left">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Flash" Width="18" Height="18" Margin="0,0,10,0" VerticalAlignment="Center" Foreground="{DynamicResource Yellow}"/>
                                <TextBlock x:Name="QuickStudyText" Text="Quick Study" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Streak / XP at bottom -->
                    <Border Grid.Row="2" BorderBrush="{DynamicResource Border}" BorderThickness="0,1,0,0" Padding="14,12">
                        <StackPanel>
                            <Grid Margin="0,0,0,6">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8" HorizontalAlignment="Center">
                                    <Border Width="36" Height="36" CornerRadius="18" Background="{DynamicResource Surface2}" BorderBrush="{DynamicResource Border}" BorderThickness="1">
                                        <TextBlock x:Name="AvatarText" Text="👤" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="School" Width="18" Height="18" Margin="0,0,8,0" VerticalAlignment="Center" Foreground="{DynamicResource Teal}"/>
                                    <TextBlock x:Name="StreakText" Text="0 day streak"
                                               FontFamily="Segoe UI" FontSize="12" FontWeight="SemiBold"
                                               Foreground="{DynamicResource Text}" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock x:Name="LevelText" Text="Lv.1"
                                           FontFamily="Segoe UI" FontSize="11" FontWeight="Bold"
                                           Foreground="{DynamicResource Teal}" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>
                            <Border Background="{DynamicResource Surface}" CornerRadius="3" Height="6">
                                <Border x:Name="XPBar" Background="{DynamicResource Teal}" CornerRadius="3"
                                        HorizontalAlignment="Left" Width="0"/>
                            </Border>
                            <TextBlock x:Name="XPText" Text="0 XP"
                                       FontFamily="Segoe UI" FontSize="10" Foreground="{DynamicResource Dim}" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- ═══════════ CONTENT AREA ═══════════ -->
            <Grid Grid.Column="1">

                <!-- HOME PAGE -->
                <views:HomeView x:Name="PageHome" DataContext="{Binding HomeVM, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <!-- SETS PAGE -->
                <views:SetsView x:Name="PageSets" Visibility="Collapsed" DataContext="{Binding SetsVM, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <!-- SET DETAIL PAGE -->
                <Grid x:Name="PageDetail" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- Detail header -->
                    <Border Grid.Row="0" Padding="32,24,32,20" BorderBrush="{DynamicResource Border}" BorderThickness="0,0,0,1" Background="{DynamicResource Surface}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                                <Button x:Name="DetailBackBtn" Style="{StaticResource GhostButton}"
                                        Padding="10,6" Click="BackToSets_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="ArrowLeft" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="Back to Sets" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                            <Grid Grid.Row="1" Margin="0,0,0,16">
                                <StackPanel>
                                    <TextBlock x:Name="DetailName" Text="Set Name"
                                               FontFamily="Segoe UI" FontSize="24" FontWeight="Bold"
                                               Foreground="{DynamicResource Text}"/>
                                    <TextBlock x:Name="DetailMeta" Text=""
                                               FontFamily="Segoe UI" FontSize="13" Foreground="{DynamicResource Muted}"
                                               Margin="0,4,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                    <Button x:Name="FavBtn" Style="{StaticResource IconButton}"
                                            Width="36" Height="36" Click="Fav_Click">
                                        <materialDesign:PackIcon Kind="StarOutline" Width="20" Height="20"/>
                                    </Button>
                                    <Button x:Name="DetailEditBtn" Style="{StaticResource GhostButton}"
                                            Padding="10,7" Click="EditSet_Click" Margin="6,0">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Pencil" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Edit" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                    <Button x:Name="DetailDeleteBtn" Style="{StaticResource DangerButton}"
                                            Padding="10,7" Click="DeleteSet_Click">
                                        <StackPanel Orientation="Horizontal">
                                            <materialDesign:PackIcon Kind="Delete" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                            <TextBlock Text="Delete" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                            <!-- Study mode buttons -->
                            <StackPanel Grid.Row="2" Orientation="Horizontal">
                                <Button Style="{StaticResource PrimaryButton}" Click="StartFlash_Click" Margin="0,0,10,0">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Cards" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="DetailFlashcardsText" Text="Flashcards" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource GhostButton}" Click="StartLearn_Click" Margin="0,0,10,0">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Head" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="DetailLearnText" Text="Learn" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource GhostButton}" Click="StartTest_Click" Margin="0,0,10,0">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="PencilBox" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="DetailTestText" Text="Test" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Style="{StaticResource GhostButton}" Click="StartTimed_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="TimerSand" Width="16" Height="16" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock x:Name="DetailTimedText" Text="Timed" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <!-- Word Search Bar -->
                    <Border Grid.Row="1" Margin="32,14,32,0" Background="{DynamicResource Surface}"
                            BorderBrush="{DynamicResource Border}" BorderThickness="1" CornerRadius="8">
                        <Grid>
                            <TextBox x:Name="DetailSearchBox" Style="{StaticResource DarkTextBox}"
                                     BorderThickness="0" Background="Transparent"
                                     TextChanged="DetailSearch_Changed"/>
                            <StackPanel x:Name="DetailSearchHintPanel" Orientation="Horizontal" IsHitTestVisible="False" VerticalAlignment="Center" Margin="12,0">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=DetailSearchBox}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>
                                <materialDesign:PackIcon Kind="Magnify" Width="16" Height="16" Foreground="{DynamicResource Dim}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="DetailSearchHint" Text="Search words in this set..." Foreground="{DynamicResource Dim}"
                                           FontFamily="Segoe UI" FontSize="13" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <ScrollViewer Grid.Row="2" Margin="32,14,32,32" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="DetailWords"/>
                    </ScrollViewer>
                </Grid>

                <!-- STUDY PAGE (flashcard/learn/test/timed) -->
                <Grid x:Name="PageStudy" Visibility="Collapsed">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <!-- Study header -->
                    <Border Grid.Row="0" Background="{DynamicResource Surface}" BorderBrush="{DynamicResource Border}"
                            BorderThickness="0,0,0,1" Padding="24,14">
                        <Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Button Style="{StaticResource GhostButton}"
                                        Padding="10,6" Click="EndStudy_Click">
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="Close" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <TextBlock Text="End" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <TextBlock x:Name="StudyTitle" Text="Flashcards"
                                           FontFamily="Segoe UI" FontSize="14" FontWeight="SemiBold"
                                           Foreground="{DynamicResource Text}" VerticalAlignment="Center" Margin="16,0"/>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                                <TextBlock x:Name="TimerText" Text=""
                                           FontFamily="Segoe UI Mono" FontSize="14" FontWeight="Bold"
                                           Foreground="{DynamicResource Yellow}" VerticalAlignment="Center" Margin="0,0,16,0"/>
                                <materialDesign:PackIcon Kind="Check" Width="14" Height="14" Foreground="{DynamicResource Teal}" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="StudyCorrectText" Text="0" FontFamily="Segoe UI" FontSize="13" Foreground="{DynamicResource Text}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <materialDesign:PackIcon Kind="Close" Width="14" Height="14" Foreground="{DynamicResource Red}" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock x:Name="StudyWrongText" Text="0" FontFamily="Segoe UI" FontSize="13" Foreground="{DynamicResource Text}" VerticalAlignment="Center" Margin="0,0,12,0"/>
                                <TextBlock x:Name="StudyCounter" Text="1/10"
                                           FontFamily="Segoe UI" FontSize="13" Foreground="{DynamicResource Muted}"
                                           VerticalAlignment="Center"/>
                            </StackPanel>
                        </Grid>
                    </Border>
                    <!-- Progress bar -->
                    <Border x:Name="ProgressBorder" Grid.Row="0" VerticalAlignment="Bottom"
                            Height="3" Background="{DynamicResource Surface}" Margin="0,0,0,0">
                        <Border x:Name="ProgressFill" Background="{DynamicResource Teal}" HorizontalAlignment="Left" Width="0"/>
                    </Border>
                    <!-- Study content -->
                    <Grid Grid.Row="1" x:Name="StudyContent"/>
                </Grid>

                <!-- STATISTICS PAGE -->
                <views:StatsView x:Name="PageStats" Visibility="Collapsed" DataContext="{Binding StatsVM, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <!-- BADGES PAGE -->
                <views:BadgesView x:Name="PageBadges" Visibility="Collapsed" DataContext="{Binding BadgesVM, RelativeSource={RelativeSource AncestorType=Window}}"/>

                <!-- PROFILE PAGE -->
                <views:ProfileView x:Name="PageProfile" Visibility="Collapsed" DataContext="{Binding ProfileVM, RelativeSource={RelativeSource AncestorType=Window}}"/>

            </Grid>
        </Grid>
    </Border>
</Window>
